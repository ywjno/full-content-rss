# encoding: utf-8

require 'rubygems'

require File.expand_path('../lib/database', __FILE__)
require File.expand_path('../lib/crawl', __FILE__)

desc "init data"
task :init do
  Crawl.init
end

desc "update data"
task :update do
  Crawl.update
end
